#include<stdio.h>
int main()
{
	int a[5]={2,3,5,7,9};
	int i, max,min;
	max=min=a[0];
	for(i=0;i<5;i++)
	{
		if(max<a[i])
		
			max=a[i];
		
		if(min>a[i])
		
			min=a[i];
		
	}
	printf("the maximum number of array: %d\n",max);
	printf("the minmum number of array: %d\n ",min);
}
